package at.gv.egiz.android.signature.verification;

/**
 * Verifies a given signature.
 * 
 * @author sandra.kreuzhuber@iaik.tugraz.at
 * 
 */
public interface SignatureVerifier {

	/**
	 * Verifies a given signature.
	 * 
	 * @param signature
	 *            - a string containing the XMLDsig signature
	 * @return VerificationResult
	 */
	public VerificationResult verify(String signature);

}
